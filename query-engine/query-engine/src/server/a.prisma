datasource db {
  provider = "mysql"
  url      = "mysql://root:123456,zjl@localhost:3306/yxy"
}

model shechem_common_sms_log {
  id   Int @id @default(autoincrement())
  uid  Int @default(0) @map("_uid")
  uid_ Int @default(0)
}
